from setuptools import setup


setup(
    name='filewrap',
    version='1.0',
    description='Library providing unified interface for remote and local directory/file access',
    packages=['filewrap'],
)
